# Magic Square 프로젝트 — KPT 회고 (Prompt 01\~27 종합)

> \*\*회고 대상:\*\* `Prompt/` 폴더의 26개 프롬프트 (01\~27, 14번 결번)
> \*\*방법론:\*\* KPT (Keep · Problem · Try) + 학습 매트릭스
> \*\*작성일:\*\* 2026-04-28
> \*\*근거 문서:\*\* `Prompt/01\_...` \~ `Prompt/27\_...`, `Reporter/01\~27`, `docs/PRD\_MagicSquare\_4x4\_TDD.md`, `.cursorrules`

\---

## K — Keep (잘 됐던 것 · 계속 유지할 것)

|#|항목|근거 (Prompt)|
|-|-|-|
|K1|**단계적 문서 빌드업** — 문제 인식 → 설계 → 규칙 → 스토리/시나리오 → PRD → To-Do → README 순으로 누적|01 → 02 → 03\~07 → 09\~15 → 16\~17 → 18\~20|
|K2|**모든 세션 끝에 Reporter 보고서 산출** (번호·메타·이력 일관)|01\~27 전체 공통 패턴|
|K3|**불변조건(Invariant) 중심 사고** — “마방진을 만든다” 대신 “조건을 만족하는 완성 상태로 판정” 관점 고정|01, 09, 16|
|K4|**입력·출력 계약 고정** (`int\[]\[]`, 빈칸 정확히 2, `int\[6]` 1-index, 시도1→시도2 정책)|02, 16, 24|
|K5|**ECB(Entity–Control–Boundary) 일관 적용** — `.cursorrules`·아키텍처·테스트 경로 모두 미러 구조|05, 07, 18, 24|
|K6|**Dual-Track UI + Logic TDD** — Track A(UX Contract) ↔ Track B(Logic) 독립 + 계약으로만 연결|06, 17, 24|
|K7|**Concept → Rule → UC → Contract → Test → Component 추적성 매트릭스**|02, 16, 17, 18, 20|
|K8|**Red → Green → Refactor 한 사이클 1커밋 단위** 강제 + Conventional Commit|24, 27|
|K9|**매직 넘버 상수화 의지** (`MATRIX\_SIZE`, `MAGIC\_CONSTANT=34`, `EMPTY\_CELL\_VALUE`)|03\~07, 24, 26|
|K10|**venv + pytest + GitHub Flow(짧은 브랜치 `red`/`green`)** 워크플로 정착|21, 22, 23, 24|
|K11|**TDD 위반 시 즉시 `\[TDD\_RULE\_WARNING]` 자기 신고** 메커니즘|08, `.cursorrules` ai\_behavior|

\---

## P — Problem (문제였던 것 · 개선 필요한 것)

|#|항목|근거 (Prompt)|
|-|-|-|
|P1|**TDD 순서 위반 발생** — User 엔티티를 테스트와 구현을 동시에 한 번에 작성 (Red 누락)|08 — 응답 끝의 `\[TDD\_RULE\_WARNING]`|
|P2|**GUI 초기값 요구사항 충돌 반복** — “0 두 칸” ↔ “모든 합 34” 동시 만족 불가, 같은 자리에서 4\~5회 왕복 수정|24 (그리고 25 헤더 메시지 보강)|
|P3|**`solve\_two\_blanks()` 한 함수에 검증 + 누락값 계산 + 탐색이 혼재** (SRP 위반, 76\~120행)|26|
|P4|**`gui/app.py`의 `main()`이 위젯·레이아웃·스타일·핸들러·도메인 호출까지 모두 처리** (14\~185행)|26|
|P5|**매직 넘버·문자열 잔존** — 에러 메시지 안의 `"1 to 16"`, GUI 셀 크기·색상·폰트 픽셀값|26|
|P6|**ECB 표기 혼란** — TASK-008에서 “Control vs Domain Service” 결정 못 하고 본문 안에서 갈팡질팡 (출력에 사고 흔적이 노출)|18 (TASK-008\~012 주변)|
|P7|**환경 문제 반복 발생** — `pytest` PATH 누락, PowerShell `ExecutionPolicy` 차단, 전역 Python(3.14) vs venv 혼선|22, 23, 24|
|P8|**CI/품질 게이트 미구현** — 커버리지 임계, ArchUnit, Job A/B 분리, `domain` 직접 import 금지 모두 “계획만” 존재|17, 18 (TASK-023\~025 미체크)|
|P9|**Reporter 번호 충돌** — `12\_` 접두사가 Level3 export·Level4 작업본 두 개 존재|13 (Level4 export를 13으로 회피)|
|P10|**README ↔ Reporter ↔ Prompt 동기화가 수동** (문서 맵 갱신 누락 빈번, 19/20/22/23번이 README에 반영 안 됨)|19, 20, 22, 23|
|P11|**`AMBIGUOUS\_SOLUTION` 정책 미결정** — 둘 다 성공/둘 다 실패 케이스 정책이 “학습용 보류”|02, 16 (DEC-04 보류)|
|P12|**PRD 1.0 → 1.2로 두 번 보강이 필요했던 누락** — `INV-C1\~C5` 미정의, 잘못된 §참조, `BR-ERR-01` 오타(공밍→공백)|17|

\---

## T — Try (다음에 시도할 것)

|#|항목|기대 효과 / 근거|
|-|-|-|
|T1|**ArchUnit(또는 import-linter)로 ECB 경계 자동 검증** — Track A 테스트가 `magicsquare.domain` 직접 import 시 빌드 실패|P8, TASK-025|
|T2|**CI 커버리지 게이트** — Domain ≥95% / Boundary ≥85% 미달 시 머지 차단 (`pytest-cov` + GitHub Actions)|16\~18 NFR-01·02, 24 (커버리지 안내)|
|T3|**CI Job A(UX Contract) / Job B(Logic) 분리 + 로그 접두사** `\[ux-contract]`/`\[logic]`|17 §8.5.1, §9.5|
|T4|**`solve\_two\_blanks()` Extract Function** — `validate → find\_missing → try\_placement(order)` 3단계로 분해|26 (R-L4 후보), 27이 R-L2만 처리하고 멈춤|
|T5|**`Board` / `CellCoordinate` / `SolutionVector` Value Object 도입** — 1-index/blank=0 규약을 Entity로 끌어올림|26 §3|
|T6|**GUI 책임 분리** — `format\_board\_lines`·`update\_sums\_label`을 boundary의 `apply\_solution\_vector(grid, vec)` + presenter로 이동|26 (R-U3, Move Function)|
|T7|**pre-commit hook으로 `print()` / 매직 넘버 / `except:` 차단** — `.cursorrules` `forbidden`을 자동 강제|05\~07 forbidden 섹션|
|T8|**`AMBIGUOUS\_SOLUTION` 정책 확정** — “둘 다 성공이면 거부”인지 “시도1 우선”인지 결정·테스트 고정|P11, 02 UC-D05|
|T9|**README 문서 맵 자동 갱신 스크립트** — Reporter/Prompt 신규 추가 시 한 줄 자동 삽입|P10|
|T10|**Stacked PR 또는 `feature/4x4-tdd` 통합 브랜치 도입** — RED 브랜치만 main에 머지될 때 CI 빨강 방지|24 (브랜치 전략 §3)|
|T11|**GUI 스모크 테스트** — `python -m magicsquare.gui` import·핵심 핸들러 1회 호출 (PyQt 위젯 단언 없이)|26|
|T12|**속성/불변량 기반 테스트(property test)** — Hypothesis로 1\~16 순열·합 34 invariant 자동 생성|K3, 16 §9 Property/Invariant|
|T13|**에러 메시지 단일 SoT(`ErrorMessages` enum)** — domain의 `"1 to 16"` 문자열을 상수에서만 생성|26 우선순위 4|

\---

## 한 줄 회고

* **잘된 것:** 문서·계약·추적성·1커밋 단위 TDD 사이클이 27회 동안 끊김 없이 누적된 것.
* **아픈 것:** GUI 요구사항이 동시에 만족 불가능한 형태로 반복 충돌했고, SRP 위반이 누적되며 26번에서야 식별된 것.
* **다음 한 수:** 27번이 시작한 `R-L2` 추출 흐름을 `R-L4`(시도 분리) → `R-U2`(메시지 단일화) → CI 게이트 순으로 이어 가는 것.

\---

## 문서 이력

|일자|버전|변경 내용|
|-|-|-|
|2026-04-28|1.0|Prompt 01\~27 회고를 KPT(Keep·Problem·Try) 매트릭스로 정리해 최초 작성|



